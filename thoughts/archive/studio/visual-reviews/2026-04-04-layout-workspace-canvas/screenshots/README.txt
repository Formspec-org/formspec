Visual review target: Formspec Studio — Layout workspace (LayoutCanvas + tree).

Pass 1 PNGs were not captured in this automated run (no dev server + browser session).
To add screenshots:
  1. npm run test:serve (or start Studio) and open the Layout tab.
  2. Save captures as:
     - pass1-01-initial.png (canvas with mixed containers + fields)
     - pass1-02-field-selected.png (field selected, toolbar + summary visible)
     - pass1-03-grid-context.png (fields inside Grid)

DOM snapshots: see dom-01-initial.txt (synthetic outline from source).
