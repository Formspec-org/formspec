{
  "name": "formspec-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:test-server": "vite --port 8080 --host 127.0.0.1",
    "test": "playwright test",
    "docs:generate": "node scripts/generate-spec-artifacts.mjs",
    "docs:check": "node scripts/generate-spec-artifacts.mjs --check && python3 -m pytest tests/test_cross_spec_contracts.py -q"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "chevrotain": "^11.1.1"
  }
}
