{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://formspec.org/schemas/token-registry/1.0",
  "title": "Formspec Token Registry",
  "description": "A structured catalog of design tokens with metadata for tooling, validation, and studio consumption. The registry is a development artifact — renderers MUST NOT require it at runtime.",
  "type": "object",
  "required": ["$formspecTokenRegistry", "categories"],
  "additionalProperties": false,
  "properties": {
    "$formspecTokenRegistry": {
      "type": "string",
      "const": "1.0",
      "description": "Token Registry specification version. MUST be '1.0'. Processors MUST reject documents with an unrecognized version."
    },
    "description": {
      "type": "string",
      "description": "Human-readable description of this registry."
    },
    "categories": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "$ref": "#/$defs/Category"
      },
      "description": "Token categories keyed by category prefix (e.g., 'color', 'spacing', 'font'). Each token entry key within a category MUST start with the category key followed by a dot."
    }
  },
  "$defs": {
    "Category": {
      "type": "object",
      "required": ["type", "tokens"],
      "additionalProperties": false,
      "description": "A group of tokens sharing a category prefix and default type.",
      "properties": {
        "description": {
          "type": "string",
          "description": "Human-readable description of the category."
        },
        "type": {
          "$ref": "#/$defs/TokenType",
          "description": "Default token type for entries in this category. Entries may override with their own type."
        },
        "darkPrefix": {
          "type": "string",
          "description": "Prefix for dark-mode counterpart tokens. When present, each token's 'dark' field holds the default dark-mode value. The derived dark key is '<darkPrefix>.<suffix>' where suffix is the token key with the category prefix and its trailing dot removed. Only categories whose type is 'color' SHOULD declare darkPrefix."
        },
        "tokens": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "$ref": "#/$defs/TokenEntry"
          },
          "description": "Token entries keyed by their full dot-delimited key. Each key MUST start with the category key followed by a dot."
        }
      }
    },
    "TokenEntry": {
      "type": "object",
      "additionalProperties": false,
      "description": "Metadata for a single design token.",
      "properties": {
        "description": {
          "type": "string",
          "description": "Human-readable description of the token's purpose."
        },
        "type": {
          "$ref": "#/$defs/TokenType",
          "description": "Token type, overriding the category default."
        },
        "default": {
          "oneOf": [
            { "type": "string" },
            { "type": "number" }
          ],
          "description": "Default value shipped with the platform theme."
        },
        "dark": {
          "oneOf": [
            { "type": "string" },
            { "type": "number" }
          ],
          "description": "Default dark-mode value. Only meaningful when the containing category declares a darkPrefix. Processors MUST ignore this field when darkPrefix is absent and SHOULD emit a warning."
        },
        "examples": {
          "type": "array",
          "items": {
            "oneOf": [
              { "type": "string" },
              { "type": "number" }
            ]
          },
          "description": "Example token values for documentation and tooling hints."
        }
      }
    },
    "TokenType": {
      "type": "string",
      "enum": [
        "color",
        "dimension",
        "fontFamily",
        "fontWeight",
        "duration",
        "opacity",
        "shadow",
        "number"
      ],
      "description": "Semantic token type. Tooling uses types to select appropriate editors and validate values. Types do not affect CSS custom property emission."
    }
  }
}
